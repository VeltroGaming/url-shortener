{
  "name": "url-shortener",
  "version": "0.0.0",
  "description": "Minimalistic URL Shortener",
  "main": "index.js",
  "scripts": {
    "start": "iojs --use_strict start.js",
    "test": "iojs --use_strict start.js --create create --default https://www.veltro.de/ --host dev:3000 --port 3000"
  },
  "author": "Jan Buschtöns",
  "license": "MIT",
  "dependencies": {
    "babel": "^5.6.23",
    "lowdb": "^0.10.2",
    "optimist": "^0.6.1"
  }
}
